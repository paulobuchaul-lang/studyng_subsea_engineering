# src/templates/

Destino dos templates de página (hub e capítulo) que o `src/build.py` usará para gerar o site a partir de `src/content/` e `src/data/`, conforme D-005.

**Por que esta pasta está vazia:** D-005 (adotar o pipeline de build) ainda está pendente de decisão do Paulo em `/governanca/DECISOES.md`. Criar templates e o script de build antes dessa decisão arrisca retrabalho caso o Paulo prefira manter HTML editado manualmente (a alternativa descrita na própria D-005).
