# src/data/

Destino de `glossario.json`, `prompts.json`, `aliases.json` e `biblioteca.json`, conforme o Sprint 1 do roadmap (`/governanca/ROADMAP_V4.md`).

Na V3, esses dados já existem estruturados dentro de `assets/data.js` (126 termos de glossário, 28 prompts, 38 aliases). A extração é principalmente uma conversão de formato, não uma reescrita de conteúdo.

**Por que esta pasta está vazia:** `assets/data.js` da V3 não foi reenviado nesta sessão (só a governança chegou). Ver `/governanca/ESTADO_ATUAL.md` e item B-028 em `/governanca/BACKLOG.md`.
